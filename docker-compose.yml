services:
  dev:
    image: node:24-slim
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "0.0.0.0:4322:4322"
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: sh -c "npm install && npm run dev -- --host"
