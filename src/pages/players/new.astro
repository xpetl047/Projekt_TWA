---
import AuthGuard from '../../components/AuthGuard.astro';
import '../../styles/app.css';
import '@sg-styles/tokens.css';
import '@sg-styles/typography.css';
import '@sg-styles/components.css';
---

<html lang="cs">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nový hráč</title>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3/dist/cdn.min.js"></script>
  </head>
  <body>
    <AuthGuard />

    <div class="container" style="padding-top: 4rem; padding-bottom: 4rem; max-width: 64rem;">
      <h1 style="font-size: 2.4rem; margin-bottom: 2.4rem;">Nový hráč</h1>

      <div class="card" x-data="ticketCreate()">
        <form @submit.prevent="createTicket">
          <div class="form-group">
            <label for="name">Název</label>
            <input class="input" type="text" id="name" name="name" required />
          </div>

          <div class="form-group">
            <label for="description">Popis</label>
            <textarea class="input" id="description" name="description" rows="4" style="resize: vertical;"></textarea>
          </div>

          <div class="form-group">
            <label for="status">Status</label>
            <select class="input" id="status" name="status">
              <option value="active">active</option>
              <option value="inactive">inactive</option>
            </select>
          </div>

          <div class="form-group">
            <label for="role">Role</label>
            <select class="input" id="role" name="role">
              <option value="C">C</option>
              <option value="LW" selected>LW</option>
              <option value="RW">RW</option>
              <option value="D">D</option>
              <option value="G">G</option>
            </select>
          </div>

          <div class="form-group">
            <label for="assignee">Přiřazeno</label>
            <input class="input" type="text" id="assignee" name="assignee" value="admin" />
          </div>

          <div style="display: flex; gap: 1.2rem; margin-top: 2.4rem;">
            <button type="submit" class="btn btn-primary" :disabled="saving">
              <span x-show="!saving">Vytvořit</span>
              <span x-show="saving">Vytváření...</span>
            </button>
            <a href="/players" class="btn" style="background: #e5e7eb; color: #374151;">Zrušit</a>
          </div>
        </form>
      </div>
    </div>
    <script is:inline>
      window.ticketCreate = function() {
        return {
          saving: false,

          async createTicket(event) {
            if (this.saving) return;

            try {
              this.saving = true;
              const formData = new FormData(event.target);
              
              const response = await fetch('/api/tickets', {
                method: 'POST',
                body: formData
              });

              if (response.ok) {
                window.location.href = '/tickets';
              } else {
                alert('Nepodařilo se vytvořit ticket');
              }
            } catch (err) {
              alert('Chyba při vytváření: ' + err.message);
            } finally {
              this.saving = false;
            }
          }
        };
      };
    </script>
  </body>
</html>
