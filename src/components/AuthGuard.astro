---
// src/components/AuthGuard.astro
---
<script>
  (() => {
    try {
      const s = JSON.parse(localStorage.getItem('trener_session') || 'null');
      if (!s?.id) window.location.replace('/login');
    } catch { window.location.replace('/login'); }
  })();
</script>
